"""UI package for the Session 8 Streamlit dashboard.

Modules:
    styles      — CSS (light + dark palettes, glass cards, typography)
    components  — small reusable widgets (badges, metric cards, toast helper)
    runtime     — subprocess management + on-disk session reading
    sidebar     — left rail (configuration, history, theme)
    query_card  — main query input
    tabs        — Final Answer / Reasoning / Tool Calls / Logs / Metrics
"""
